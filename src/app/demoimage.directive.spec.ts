import { DemoimageDirective } from './demoimage.directive';

describe('DemoimageDirective', () => {
  it('should create an instance', () => {
    const directive = new DemoimageDirective();
    expect(directive).toBeTruthy();
  });
});
